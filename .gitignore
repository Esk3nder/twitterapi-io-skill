# Never commit credentials. The key belongs in the environment only.
.env
*.key
secrets*

# Local cache: contains fetched X data and is machine-specific.
*.db
*.db-wal
*.db-shm

# Crawl output
*.jsonl

__pycache__/
*.pyc
.DS_Store

# Agent tooling installed into this directory by `npx skills add`.
# Unrelated to the skill itself; landed here because it was the cwd.
.agents/
.claude/
skills-lock.json
